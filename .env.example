DATABASE_URL="postgresql://user:password@localhost:5432/axecompta?schema=public"

# Base dédiée aux tests d'intégration (npm test) — jamais la base de
# développement/démo, pour ne jamais perdre de données réelles à l'exécution.
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/axecompta_test?schema=public"

JWT_SECRET="change-me-in-production"
JWT_REFRESH_SECRET="change-me-too-in-production"

# Copilote IA (OCR facture/reçu + chat CFO) — voir lib/ai/gemini.ts.
# Généré depuis https://aistudio.google.com/apikey
GEMINI_API_KEY=
